# PRD — 서울 아파트 매매 시장 대시보드

**작성일** 2026-08-12 · **기획** Opus · **구현** Sonnet · **배포** GitHub Pages

---

## 1. 한 줄 정의

서울 25개 구·316개 동의 최근 12개월 아파트 **매매** 실거래를 분석해,
**"어디가 올랐고, 내 예산으로 어디에 갈 수 있으며, 진입장벽은 어떻게 이중으로 작동하는가"**
를 한 페이지에서 답하는 분석용 대시보드.

## 2. 페르소나 & JTBD

**페르소나** — 부동산 데이터 분석가 / 기획자
숫자를 있는 그대로 읽고 싶어하며, 근거 없는 단정이나 과장된 시각화를 신뢰하지 않는다.
표본 수와 계산식이 드러나 있어야 결과를 인용할 수 있다.

**Jobs to be done**

| # | Job | 대응 섹션 |
|---|-----|----------|
| J1 | 서울 시장이 지금 어느 방향인지 30초 안에 파악한다 | S1, S2 |
| J2 | 어느 지역이 오르고 어느 지역이 빠지는지 순위로 확인한다 | S3, S4 |
| J3 | 특정 예산으로 진입 가능한 지역을 추린다 | S5 |
| J4 | 관심 구를 골라 동 단위로 파고든다 | S6 |
| J5 | 전세가율로 실질 진입장벽(갭)을 확인한다 | S7 |

## 3. 데이터

**원본** `seoul-apt-latest.csv` (27.5MB, 320,141행)
**기간** 2025-07 ~ 2026-06 (12개월)
**컬럼** `contract_ym, contract_date, gu, dong, complex, area_m2, floor, deal_type, price, deposit, monthly_rent, price_per_pyeong`

| 구분 | 건수 | 대시보드에서의 역할 |
|------|------|-------------------|
| 매매 | 72,459 | **주 데이터.** 모든 KPI·추이·랭킹·예산탐색의 기준 |
| 전세 | 129,649 | 전세가율 계산에만 사용 (S7 한정) |
| 월세 | 118,033 | **미사용** |

**주의 — CSV 파싱**
단지명에 쉼표가 포함된 행이 존재한다(`"○○아파트(1,206동)"`).
단순 `split(',')`은 약 3,000행을 깨뜨린다. **따옴표를 처리하는 파서를 반드시 사용할 것.**

## 4. 검증된 핵심 인사이트

> 아래 수치는 기획 단계에서 원본 데이터로 직접 계산해 검증했다.
> 구현 결과가 이 값과 크게 다르면 집계 로직에 버그가 있는 것이다.

### 4.1 시장은 양극화됐다 (헤드라인)

H1(2025-07~12) 대비 H2(2026-01~06) 중위 평당가 변화율:

| 상승 상위 | 변화율 | 하락 하위 | 변화율 |
|-----------|--------|-----------|--------|
| 서초구 | **+21.3%** | 서대문구 | **-9.1%** |
| 송파구 | +19.7% | 은평구 | -4.8% |
| 용산구 | +17.1% | 양천구 | -4.5% |
| 성동구 | +16.6% | 종로구 | -3.8% |
| 강남구 | +13.9% | 강북구 | -1.3% |

25개 구 전부 산출 가능. 동 단위는 211/340개 산출 가능.

### 4.2 진입장벽은 이중이다 (전세가율)

**평당가 ↔ 전세가율 상관계수 r = -0.787** (n=214, 동 단위) — 강한 역상관.
비싼 지역일수록 전세가율이 낮아, 매매가가 높은 데다 갭까지 커진다.

| 전세가율 최저 | | 전세가율 최고 | |
|---|---|---|---|
| 강남구 압구정동 | **16.0%** | 금천구 가산동 | **90.5%** |
| 영등포구 여의도동 | 23.8% | 광진구 화양동 | 87.4% |
| 용산구 원효로4가 | 26.5% | 은평구 대조동 | 79.2% |

**⚠ 정직하게 쓸 것 —** 상승률 ↔ 전세가율 상관은 **r = -0.276으로 약하다.**
"많이 오른 곳일수록 갭이 크다"고 **단정하지 말 것.** 구 단위에서 그렇게 보이는 것은
가격 수준이 매개하기 때문이며, 동 단위로 내려가면 관계가 흐려진다.
S7 문안은 "**비싼 곳일수록** 갭이 크다"로 쓰고 두 r값을 모두 표기한다.

### 4.3 예산별 진입 가능 지역

거래의 30% 이상이 예산 이내인 동의 개수:

| 예산 | 진입 가능 동 |
|------|-------------|
| 5억 | 33개 |
| 8억 | 98개 |
| 10억 | 151개 |
| 15억 | 216개 |

## 5. 차별화 — 참고 사이트 대비

[ggplab.github.io/seoul-apt-trends](https://ggplab.github.io/seoul-apt-trends/) 는 동일 데이터·동일 기간의 대시보드다.
아래 3개는 **참고 사이트에 없으며**, 우리 대시보드의 존재 이유다.

| # | 우리만 있는 것 |
|---|---------------|
| D1 | **지역별 상승/하락률 랭킹** — 참고본은 월별 추이만 있고 지역 간 변화율 비교가 없다 |
| D2 | **예산 기반 역탐색** — "예산 → 갈 수 있는 동" 방향의 탐색 |
| D3 | **전세가율(갭) 분석** — 매매 데이터만으로는 보이지 않는 실질 진입장벽 |

참고본에 있는 요일별 거래량은 분석적 가치가 낮다고 판단해 **채택하지 않는다.**

## 6. 정보 구조

### 상단 — 요약 리포트 (스크롤 없이 읽히는 영역)

**S0 · 헤더**
제목, 데이터 기간(2025-07~2026-06), 출처, 총 매매 72,459건 명시.

**S1 · KPI 4장**
- 총 매매 건수 `72,459건`
- 중위 매매가 `95,000만원`
- 중위 평당가 (전체 기간 기준)
- 반기 변화율 (서울 전체, H1→H2)

각 카드에 값 + 라벨 + 보조설명. 변화율은 부호에 따라 색이 바뀐다.

**S2 · 헤드라인 인사이트**
"서울 아파트 시장은 2026년 상반기 양극화됐다" 를 한 문장으로 제시하고,
바로 아래 **구별 상승률 다이버징 바 차트**(25개, 0 기준 좌우 발산)로 근거를 보인다.
상승은 파랑 계열, 하락은 붉은 계열. 정렬은 변화율 내림차순.

**S3 · 월별 거래량 · 평당가 추이**
12개월. 막대(거래 건수) + 꺾은선(중위 평당가) 콤보.
서울 전체 기본, 구 선택 시 해당 구로 갱신.

**S4 · 상승/하락 지역 랭킹**
좌: 상승 TOP 10 동 / 우: 하락 BOTTOM 10 동.
각 행에 동명, 변화율, 거래 건수, 중위 평당가. 표본 15건 미만 동은 제외.

### 하단 — 인터랙티브 탐색

**S5 · 예산 탐색기** *(D2)*
입력: 예산(슬라이더 또는 직접 입력, 3억~50억, **5,000만원 단위로 선택**) + 면적대(전체/소형/중소형/중형/대형).
출력: 진입 가능 동 테이블 — 동명, 중위가, **진입 가능성 %**, 거래 건수, 반기 변화율.
정렬 기본값은 진입 가능성 내림차순. 진입 가능성은 막대로 함께 표현한다.

**S6 · 구 → 동 드릴다운**
구 선택 → 해당 구의 동별 테이블(중위가 / 중위 평당가 / 거래량 / 반기 변화율 / 전세가율).
컬럼 클릭으로 정렬 가능. 값이 없는 셀은 `—`로 표시하고 빈칸으로 두지 않는다.

**S7 · 전세가율(갭) 분석** *(D3)*
산점도: X축 = 중위 평당가, Y축 = 전세가율, 점 = 동(n=214).
r = -0.787 을 명시하고 추세선을 그린다.
보조로 상승률 vs 전세가율(r = -0.276)을 제시하되, **약한 상관임을 문장으로 밝힌다.**

**S8 · 푸터 — 데이터 한계**
- 실거래 신고 기한 30일 → 최근 1~2개월 건수는 추후 증가할 수 있음
- 전세가율은 동일 단지·동일 면적대 매칭 결과의 중앙값이며, 매칭 표본이 없는 동은 제외됨
- 반기 변화율은 거래 구성(면적·단지 믹스) 변화의 영향을 받으며 동일 물건의 가격 변동이 아님

## 7. 지표 정의

구현은 아래 정의를 **그대로** 따른다. 임의 변경 금지.

| 지표 | 계산식 | 최소 표본 |
|------|--------|----------|
| 중위 매매가 | `median(price)` | 1건 |
| 중위 평당가 | `median(price_per_pyeong)` | 1건 |
| 반기 변화율 | `median(평당가 H2) / median(평당가 H1) - 1`<br>H1 = 2025-07~12, H2 = 2026-01~06 | 구: 각 반기 30건<br>동: 각 반기 15건 |
| 진입 가능성 | `예산 이하 거래 건수 / 전체 거래 건수 × 100` | 동 10건 |
| 전세가율 | 동일 `(구, 동, 단지, 면적대)` 그룹에서<br>`median(전세 deposit) / median(매매 price) × 100`<br>→ 그룹별로 구한 뒤 **동 단위 중앙값** | 그룹: 매매 3건 & 전세 3건<br>동: 3개 그룹 이상 |

**면적대 구분** — 소형 `<60㎡` / 중소형 `60~85` / 중형 `85~102` / 대형 `≥102`

**전세가율은 반드시 단지 단위로 매칭할 것.**
구×면적대로만 매칭하면 중구 소형이 119.3%로 나오는 등 왜곡이 발생한다.
단지 단위 매칭 시 100% 초과 이상치는 0개다.

**표본 미달 처리** — 값을 추정하지 말고 `null`로 두고 UI에서 `—`로 표시한다.

## 8. 기술 스택

GitHub Pages는 정적 호스팅이므로 **27.5MB CSV를 런타임에 읽지 않는다.**
빌드 시점에 집계해 작은 JSON으로 굽는다.

```
seoul-apt-latest.csv  ──[ build/aggregate.js ]──▶  data/*.json  ──▶  브라우저
      27.5MB                 Node, 의존성 0            ~300KB
```

| 레이어 | 선택 | 이유 |
|--------|------|------|
| 집계 | Node 스크립트 (외부 의존성 0) | CI에서 `node build/aggregate.js` 한 줄로 실행 |
| 프레임워크 | 없음 (vanilla ES modules) | 정적 페이지에 번들러·빌드체인 불필요 |
| 차트 | **직접 만든 인라인 SVG + CSS 바** | CDN 의존 0, Montage 토큰 완전 제어 |
| 스타일 | CSS 커스텀 프로퍼티 | 이미 추출한 Montage 토큰 재사용 |

**차트 라이브러리를 쓰지 않는다.** 필요한 차트는 다이버징 바, 콤보(막대+선), 산점도, 랭킹 바 4종이며
모두 SVG로 직접 그릴 수 있다. 외부 CDN 장애나 디자인 시스템 불일치를 원천 차단한다.

### 데이터 계약

```
data/meta.json     생성 시각, 데이터 기간, 총 건수, 각 지표의 최소 표본 기준
data/summary.json  서울 전체 KPI + 월별 추이 12개
data/gu.json       25개 구 × { 중위가, 중위평당가, 거래량, h1, h2, 변화율, 전세가율, 월별추이 }
data/dong.json     316개 동 × { 구, 중위가, 중위평당가, 거래량, 변화율, 전세가율,
                                면적대별 { 거래량, 중위가, 가격히스토그램(5,000만원 구간) } }
```

`dong.json`의 가격 히스토그램이 S5 예산 탐색기의 진입 가능성을 계산하는 근거다.
히스토그램 구간은 **우측 폐구간 `(lo, hi]`**이다 (`binIndex bi` → `(bi*5000, (bi+1)*5000]`).
매매가가 5,000만원 단위 정수에 몰리는 특성상, 예산 입력을 이 구간 경계(5,000만원 단위)에
맞춰 선택하면 근사가 아닌 정확한 진입 가능성을 계산할 수 있다.
전체 payload 300KB 이하를 목표로 한다.

## 9. 디자인

**Wanted Montage 디자인 시스템** ([wanteddev/montage-web](https://github.com/wanteddev/montage-web)) 토큰을 따른다.
현재 `index.html`에 이미 CSS 변수로 이식돼 있으므로 **그대로 재사용**한다.

| 항목 | 값 |
|------|-----|
| Primary | `#0066FF` (blue/50), strong `#0054D1`, heavy `#003E9C` |
| Neutral | `#171717` ~ `#F7F7F7` |
| 폰트 | Pretendard Variable |
| 타이포 | display2 / title2 / heading1 / body1 / label1 / caption1 |
| Radius | 8 / 10 / 12px · Spacing 4~80px 스케일 |

**차트 색상 규칙**
- 상승 = primary blue 계열 / 하락 = 붉은 계열 — 색만으로 구분하지 말고 부호(`+`/`−`)를 함께 표기
- 순위 막대는 단색 primary 유지, 무지개 팔레트 금지
- 구현 전 **`dataviz` 스킬을 반드시 로드**하고 그 규칙을 따를 것

**반응형** — 모바일에서 표는 가로 스크롤(`overflow-x:auto`), 차트는 viewBox로 축소.
가로 스크롤이 `body`에 발생해서는 안 된다.

## 10. 배포

- **대상** `jykim0931-hue/SESAC---WEB-1430` 저장소 루트 (`index.html`)
- **기존 자기소개 페이지는 `about.html`로 보존**하고 대시보드가 루트를 차지한다
- GitHub Actions 워크플로 `.github/workflows/deploy-pages.yml` 는 이미 존재하며 재사용한다
- 워크플로에 **집계 스크립트 실행 단계를 추가**한다 (`node build/aggregate.js`)

> **⚠ 선행 조건 —** 이 저장소는 GitHub Pages가 아직 활성화되지 않아 이전 배포가
> `Setup Pages` 단계에서 실패했다. 저장소 소유자가
> **Settings → Pages → Source 를 "GitHub Actions"로** 설정해야 배포가 성공한다.
> 이 설정은 저장소 관리자만 가능하다.

## 11. 범위 밖 (Non-goals)

- 월세 데이터 분석
- 단지 단위 드릴다운 (구 → 동까지만)
- 지도 시각화 (좌표 데이터 없음)
- 가격 예측·추천
- 로그인, 즐겨찾기, 서버 API

## 12. 완료 기준

- [ ] 따옴표 처리 파서로 320,141행 전부 손실 없이 파싱된다
- [ ] §4 검증 수치가 화면 값과 일치한다 (서초 +21.3%, 압구정 16.0%, r=-0.787)
- [ ] S1~S8 전 섹션이 구현되고, D1·D2·D3 차별화 요소가 모두 동작한다
- [ ] 표본 미달 값이 추정치가 아닌 `—`로 표시된다
- [ ] S7이 두 상관계수를 모두 표기하고 약한 상관을 단정하지 않는다
- [ ] 데이터 payload ≤ 300KB, 외부 CDN 의존은 폰트 외 없음
- [ ] 모바일(375px)에서 body 가로 스크롤이 없다
- [ ] GitHub Actions 워크플로가 집계 → 배포까지 성공한다
